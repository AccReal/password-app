# Деплой на Render.com

## Шаги для деплоя:

### 1. Подготовка GitHub репозитория

1. Создай новый репозиторий на GitHub
2. Инициализируй Git в проекте (если еще не сделано):
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```
3. Подключи удаленный репозиторий:
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
   git branch -M main
   git push -u origin main
   ```

### 2. Деплой на Render.com

1. Зайди на [render.com](https://render.com) и зарегистрируйся (можно через GitHub)

2. Нажми **"New +"** → **"Web Service"**

3. Подключи свой GitHub репозиторий

4. Настрой параметры:
   - **Name**: `password-generator` (или любое другое имя)
   - **Environment**: `Docker`
   - **Region**: Выбери ближайший регион
   - **Branch**: `main`
   - **Plan**: `Free`

5. Нажми **"Create Web Service"**

6. Render автоматически:
   - Обнаружит Dockerfile
   - Соберет Docker образ
   - Запустит приложение
   - Выдаст публичный URL (например: `https://password-generator.onrender.com`)

### 3. Готово! 🎉

Твое приложение будет доступно по адресу, который выдаст Render.

**Важно:**
- Бесплатный план Render засыпает после 15 минут неактивности
- Первый запрос после сна может занять 30-60 секунд
- Для постоянной работы нужен платный план ($7/месяц)

### Обновление приложения

Просто сделай push в GitHub:
```bash
git add .
git commit -m "Update"
git push
```

Render автоматически пересоберет и задеплоит новую версию!

## Альтернативные варианты

### Railway.app
1. Зайди на [railway.app](https://railway.app)
2. Подключи GitHub репозиторий
3. Railway автоматически определит Spring Boot и задеплоит

### Heroku
1. Установи Heroku CLI
2. Выполни:
   ```bash
   heroku create
   git push heroku main
   ```

## Проверка работы

После деплоя проверь:
- Главная страница: `https://your-app.onrender.com`
- API: `https://your-app.onrender.com/api/password/generate`
- Все функции: генерация, валидация, сравнение, статистика

## Troubleshooting

Если что-то не работает:
1. Проверь логи в Render Dashboard
2. Убедись что порт 8080 используется
3. Проверь что все файлы закоммичены в Git
